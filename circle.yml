
machine:
  node:
    version: 4

dependencies:
  override:
    - make node_modules

test:
  override:
    # ensure we can build for both Duo and Browserify
    - make check-build
    - make coverage
    - make check-coverage
